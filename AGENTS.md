# AGENTS.md

本文件定义本仓库内 AI agent 与开发协作者的工作边界、输出要求与文档维护约定。

---

# 1. 仓库定位

这是一个处于早期规划阶段的项目仓库，当前重点不在已有代码，而在产品定义、页面规划、服务拆分与后续实现基线的建立。

仓库当前的核心文档：

* `project.md`：产品定位、功能范围、系统架构、服务边界、接口契约、数据模型与代码基线
* `webdesign.md`：前端页面规划、布局、交互与视觉方向
* `webview/`：视觉参考图，不是最终设计稿
* `talk.md`：协作结论、推进顺序、决策记录

Agent 在修改仓库内容时，必须先理解这几个文件之间的关系：

* `project.md` 决定做什么，以及系统怎么拆
* `webdesign.md` 决定页面怎么呈现
* `webview/` 决定视觉方向参考
* `talk.md` 决定当前阶段优先做什么

---

# 2. 当前产品共识

本项目的产品核心不是泛化题库，也不是普通聊天机器人，而是：

“围绕单题进行导入、识别、作答、解析、追问和沉淀的 AI 错题工作台”

后续任何设计、建模或编码工作，都应优先服务这条核心链路：

1. 导入题目
2. OCR 结构化
3. 展示题目并支持用户作答
4. AI 解析
5. 继续追问
6. 保存并管理错题

如果某项设计偏离这个闭环，应主动收敛范围。

---

# 3. Agent 工作原则

## 3.1 先收敛，再扩展

优先把 MVP 闭环讲清楚、做扎实，不要过早引入：

* 社区
* 分享
* 复杂权限体系
* 大而全的数据平台
* 与当前规模不匹配的服务拆分

说明：

* 当前允许引入 `Go + Gin` 与 `Python + FastAPI` 的双后端架构
* 但 Python 服务只承担 OCR 与 LLM 等耗时 AI 任务
* 不允许把所有业务都拆散成多个独立微服务

## 3.2 文档优先结构化

如果修改的是文档，优先输出：

* 明确的章节结构
* 可执行的结论
* 约束条件
* 推荐方案与理由
* 接口输入输出
* 状态流与异常处理

避免只写口号式描述。

## 3.3 前端设计必须贴合工作台场景

本项目前端默认方向：

* 深色工作台
* 三栏布局优先
* 桌面端优先
* 强调题目详情、解析、追问并行可见
* 支持题目录入状态、OCR 状态、解析状态可视化

不要把页面简化成单栏聊天页，除非用户明确要求改方向。

## 3.4 数据建模围绕题目实体

后续若开始写后端代码或数据库设计，默认以 `Question` 为中心组织：

* OCR 结果挂在题目下
* 解析结果挂在题目下
* 用户作答记录挂在题目下
* 对话记录挂在题目下
* 标签、分类、状态服务于题目检索与复习

不要把会话当作顶层业务主实体。

## 3.5 服务边界必须清晰

当前默认技术分工如下：

### 核心业务端

* `Go + Gin + MySQL`
* 负责用户、题目、解析结果、状态流转、CRUD、鉴权、网关与对前端 API

### AI 算法端

* `Python + FastAPI`
* 作为内部微服务
* 负责图片接收、OCR 调用、结构化清洗、LLM 调用、标准 JSON 输出

设计时必须遵守：

* 前端不直接调用 Python 服务
* Python 不直接承担用户体系和主业务数据库写入
* Go 作为唯一业务入口与编排层

---

# 4. 修改文档时的具体要求

## 修改 `project.md`

必须保证：

* 产品目标明确
* MVP 与长期规划分开
* 功能边界清晰
* 技术选型有理由
* Go 与 Python 的职责拆分明确
* 接口和数据模型能落地
* 同步与异步链路有清晰建议

## 修改 `webdesign.md`

必须保证：

* 写清页面用途，不只是视觉描述
* 区分左栏、中栏、右栏的职责
* 包含交互流、状态、异常和响应式策略
* 包含导入中、识别中、解析中、失败重试等状态设计
* 能直接作为原型或前端实现输入

## 使用 `webview/` 参考图时

必须注意：

* 参考的是布局与气质，不是逐像素复刻
* `demo1` 更适合作为主布局参考
* `demo2` 更适合作为中栏对话区表达参考

---

# 5. 后续若进入代码阶段的建议约定

如果后续在本仓库新增前后端代码，建议遵循：

## 前端

* `src/components` 放通用组件
* `src/features/questions` 放题目相关业务模块
* `src/features/chat` 放题目对话模块
* `src/features/import` 放上传与 OCR 流程模块
* `src/features/analysis` 放题目解析模块
* `src/pages` 放页面入口

## Go 核心业务端

* `backend/cmd/` 放服务入口
* `backend/internal/handlers` 放 HTTP handler
* `backend/internal/services` 放业务逻辑
* `backend/internal/repository` 放数据访问
* `backend/internal/models` 放实体模型
* `backend/internal/integrations/ai` 放 Python 微服务适配层

## Python AI 算法端

* `ai-service/app/main.py` 放 FastAPI 入口
* `ai-service/app/api` 放 Router
* `ai-service/app/schemas` 放请求响应模型
* `ai-service/app/services` 放 OCR 与 LLM 业务逻辑
* `ai-service/app/core` 放配置、日志、公共能力

核心原则：

* 外部能力接入与业务逻辑分层
* 不要把 OCR/LLM prompt、数据库访问和接口逻辑混写
* Python 服务返回结构化结果，Go 决定如何入库和对外返回

---

# 6. 文风与输出要求

Agent 在本仓库写文档时默认使用简体中文，要求：

* 术语准确
* 结构清楚
* 不写空泛营销语言
* 给出具体建议时尽量说明原因
* 给出接口设计时尽量包含请求、响应、状态与失败场景

在用户没有明确要求前，不要把文档写成过度学术化或过度 PR 化的风格。

---

# 7. 对话记录与 `talk.md` 约定

从当前阶段开始，仓库内的协作过程文档统一记录在 `talk.md`。

具体要求：

* 当用户要求“告诉我怎么做”“下一步怎么推进”“给我规划”时，优先把结论写入 `talk.md`
* `talk.md` 采用“最新内容在前，历史内容在后”的追加方式
* 每次新增记录时，必须从文件第一行开始写，把旧内容整体后移，而不是追加到文件末尾
* 可以把用户接下来要做的事情、Agent 建议、执行顺序、待办项都写入 `talk.md`
* 如果本轮协作产生了明确决策，Agent 应优先同步到 `talk.md`，再在对话中做简要说明

这个约定的目的，是让 `talk.md` 成为仓库内持续更新的协作入口和行动记录。

---

# 8. 禁止事项

除非用户明确要求，否则不要擅自：

* 推翻“错题工作台”这个核心方向
* 把页面改成纯聊天产品
* 让前端直接访问 Python AI 服务
* 在没有必要时继续拆出更多微服务
* 删除已有产品语义，只保留技术术语
* 用过度理想化方案替代可落地 MVP

---

# 9. 推荐协作顺序

后续继续推进本项目时，建议优先顺序为：

1. 稳定 `project.md`
2. 稳定 `webdesign.md`
3. 确认 Go/Python 服务接口契约
4. 确认前端高保真原型
5. 确认数据库与接口草案
6. 再开始项目脚手架与代码实现

在这之前，不建议直接重投入编码。

---

# 10. 结论

本仓库当前最重要的事情不是“多做功能”，而是把以下四件事对齐：

* 产品核心闭环
* 页面工作台形态
* Go 与 Python 的职责边界
* 后续实现边界

Agent 的工作应围绕这四点持续增强一致性，而不是扩散复杂度。
