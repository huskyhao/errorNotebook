# 前端页面规划说明

本文档用于约束后续原型与页面生成方向。当前 MVP 是“AI 错题复习工作台”：围绕单题导入、作答、解析、追问、错因归纳和复习推荐展开。PDF 导入、试卷拆题、PDF 批次页与校对页已经从当前页面规划移除，仅可能在未来作为独立扩展重新评估。

结合 `webview/demo1.png` 与 `webview/demo2.png`，推荐采用：

* 整体布局以 `demo1` 为主，因为题库管理、题目详情、对话三者同时可见，更符合“错题工作台”定位
* 对话呈现吸收 `demo2` 的简洁感，避免中间区域过空、信息层次不清

---

# 1. 页面定位

这不是普通聊天页，也不是纯题库列表页，而是一个“围绕单题学习”的桌面工作台。

页面需要同时承载三件事：

1. 找题
2. 看题
3. 围绕题继续追问

P1 状态补充：中栏解析卡显示 AI 自动分类与标签的来源、空结果原因和手动修改入口；追问 composer 显示上传中、视觉分析中、待补图、待复核和失败重试；“生成相似题”只展示候选预览与确认保存/放弃，不提前进入题库。练习结果中的主观题显示“AI 建议分数”、评分依据、缺失要点和不确定项，确认控件明确标注人工确认。

所以三栏布局是合理的，但必须明确主次：

* 左栏负责“检索与切换”
* 中栏负责“AI 讲解与追问”
* 右栏负责“题目事实与最终沉淀结果”

右栏是事实区，中栏是推理区，左栏是索引区。

---

# 2. 总体布局

## 推荐桌面布局

```text
┌───────────────┬──────────────────────┬──────────────────────┐
│ 左侧题库栏     │ 中间 AI 对话工作区     │ 右侧题目详情与解析区   │
└───────────────┴──────────────────────┴──────────────────────┘
```

## 三栏宽度建议

* 左栏：320px 到 380px
* 中栏：最小 520px，优先自适应
* 右栏：520px 到 640px

## 顶层结构建议

页面由四层组成：

1. 顶部全局栏
2. 左侧题库栏
3. 中部对话栏
4. 右侧题目详情栏

---

# 3. 视觉方向

## 视觉关键词

* 深色桌面
* 专注、克制、工具感
* 高密度但不压迫
* 重点操作高亮清晰

## 风格结论

建议保留深色模式，不建议做成标准 ChatGPT 白底页面。这个产品更像“学习工作台”，不是“纯聊天入口”。

## 推荐色彩方向

* 背景：深灰黑，不要纯黑
* 面板：比背景略亮一层
* 边框：低对比细线
* 主强调色：暖橙/铜色
* 成功状态：低饱和绿色
* 待处理状态：琥珀色
* 错误状态：偏红但克制

这种方向和 `demo1` 更一致，也适合“题库 + AI 工具”的混合气质。

## 设计原则

* 不追求花哨动画
* 重点操作必须一眼能看见
* 状态标签比装饰更重要
* 题目内容必须保证长文本可读性

---

# 4. 顶部全局栏

## 功能目标

承载全局导航、上传入口、视图辅助操作。

## 建议元素

* 应用 Logo / 名称
* 全局搜索入口（可选，MVP 可不做）
* 导入题目按钮
* 最近导入 / 刷新按钮
* 视图切换或帮助入口
* 用户头像/设置入口

## 交互建议

* “导入题目”必须始终可见，是全局一级操作
* 若支持拖拽上传，顶部栏不替代主上传区，只作为快捷入口

---

# 5. 左侧题库栏

左栏不是简单分类栏，而是“题库导航 + 筛选入口 + 列表摘要”。

## 5.1 功能职责

* 查看题目列表
* 搜索题干/知识点/标签
* 通过状态、题型、分类筛选
* 快速切换当前题目

## 5.2 左栏内部结构

### 区块 1：标题区

建议内容：

* 标题：题目列表
* 副标题：Question Library

### 区块 2：搜索框

支持搜索：

* 题干关键词
* 知识点
* 标签

占位文案建议：

`搜索题干、知识点、标签`

### 区块 3：快捷筛选区

建议优先展示：

* 全部
* 待校准：来自 `qualityStatus = needs_review`，可显示数量
* 待解析
* 已入库
* 已掌握

建议用胶囊按钮或 segmented control，不要用下拉框。

### 区块 4：维度筛选区

建议支持两类筛选：

* 学科：操作系统、数据结构、计算机网络、组成原理
* 题型：单选题、多选题、填空题、简答题、判断题

分类行需要支持用户创建分类的删除入口。删除入口建议在 hover 或更多操作中出现，避免干扰题目浏览；`全部题目` 和 `未分类` 不显示删除。删除前使用统一确认 Dialog，并说明“删除分类不会删除题目，题目会移动到未分类”。

### 区块 5：题目列表

每个列表卡片建议包含：

* 学科标签
* 题型标签
* 待校准标签
* 题干摘要，两到三行
* 关键知识点标签
* 更新时间或导入时间
* 当前状态

## 5.3 列表项交互

当前选中项需要明显高亮，并且切换后：

* 中栏对话上下文切换到该题
* 右栏详情切换到该题

## 5.4 空状态

无题目时建议显示：

* 空状态图形
* “上传并解析第一道题”按钮

---

# 6. 中间 AI 对话工作区

中栏是产品体验核心，不应只是普通聊天窗口。

## 6.1 功能职责

* 展示 AI 标准解析
* 承接用户围绕当前题的继续提问
* 保留当前题目的多轮上下文

## 6.2 中栏结构

### 区块 1：当前题目头部

建议包含：

* 小标题：AI Conversation / 解析对话
* 当前题目名称或题号
* 当前状态提示，例如“正在查看已入库题目”

### 区块 2：消息流

消息流建议区分三类内容：

* 系统提示：例如“已加载当前题目上下文”
* AI 标准解析卡片：首次解析结果
* 用户/AI 对话消息：后续追问

不要把首次标准解析完全伪装成聊天气泡，建议使用“解析卡片 + 追问对话”的混合形态。

### 区块 3：快捷追问区

在输入框上方放 2 到 4 个真正可用的 agent action 快捷入口，例如：

* 归纳错因：触发结构化学习状态生成并落库
* 换种讲法
* 生成相似题
* 加入复习计划

“归纳错因”不再只是把 prompt 塞进输入框，而是写入 `QuestionLearningState` 的 `mistakeReason`、`weaknessTags`、`reviewAdvice`。其他入口可以继续作为追问 prompt 或后续 agent action 演进，但不应放“刷新详情”“导入题目”等和追问无关、或没有真实行为的 chip。

### 区块 4：输入区

建议包含：

* 多行输入框
* 图片附件入口，仅允许 `image/*`
* 已选择图片的缩略图或文件名，并支持移除
* 发送按钮
* 当前对话绑定提示，例如“已关联当前题目”

输入区 placeholder 保持简短，不写成长说明文。附件和图片图标必须有真实文件选择行为，不能只是 `console.log`。

## 6.3 消息样式建议

* 用户消息在右，AI 消息在左
* AI 标准解析用大卡片承载，层级高于普通对话
* 长回答需做好段落分组和列表样式

## 6.4 关键体验要求

* 切题后要清楚提示“当前对话已切换到新题”
* 历史消息可保留，但必须以题目为边界隔离
* 输入区不能在未选题时误导用户直接聊天

未选题时文案建议：

`请先选择或导入一道题目，再开始解析对话`

---

# 7. 右侧题目详情与解析区

右栏是“标准答案面板”，负责承载可沉淀、可校准、可入库的正式信息。

## 7.1 功能职责

* 展示题目原文与选项
* 展示 OCR 状态、解析状态、入库状态
* 展示结构化质量提示，例如选项可能缺失、OCR 置信度低、LLM 校验失败
* 展示标准答案、解析、知识点、标签
* 支持手动校准题干、题型、答案和选项
* 展示学科分类与知识点标签，并支持人工修改

右栏不展示“学习状态、错因、薄弱标签、复习建议”编辑区。这些内容属于跨题目的复习反馈或中栏 AI 动作结果，保留在后端和统计/归档数据流中，不在首页详情重复占据空间。移除后右栏按“题目事实 → 解析 → OCR 原文”连续布局。

## 7.2 右栏结构

### 区块 1：题目头部

建议展示：

* 题目标题或题号
* 题型
* 学科
* 状态标签
* 操作按钮

操作按钮建议优先级：

* 重新解析
* 编辑校准
* 确认入库
* 查看原图

### 区块 2：原题卡片

建议内容：

* 题干
* 选项
* 附图或 OCR 截图预览

编辑校准模式必须支持：

* `stem`
* `questionType`
* `correctAnswer`
* `options`

选项编辑需要支持 A/B/C/D 等 key 修改、选项内容修改、新增、删除，并保持 `sortOrder` 稳定。保存后仍通过 Go 的 `PATCH /questions/:id`，前端不直连 Python。

题干排版要求：

* 宽行距
* 长文本分段
* 数学公式或图片区域保留占位

### 区块 3：答案区

建议固定展示，不建议默认折叠隐藏。

原因很简单：这类页面的核心目的就是看题与看解析，默认隐藏会增加一次无意义操作。

建议内容：

* 正确答案
* 题目难度（可选）
* 知识点

### 区块 4：标准解析区

建议拆为可折叠分组，而不是一整段正文：

* 核心结论
* 逐项分析
* 易错点
* 延伸知识

### 区块 5：标签与元数据区

建议包含：

* 标签
* 来源
* 导入时间
* 最后解析时间
* 状态流转记录（后续可选）

分类与标签必须分层表达：每题只选一个“学科分类”，例如数据结构、计算机组成原理、操作系统、计算机网络；标签可选多个，用于 TCP、生成树、进程调度、拥塞控制等知识点检索。分类选择器使用单选下拉框，标签使用可搜索、多选 chip。AI 只从大类学科候选中选择分类，并给出约 3 个细知识点标签；Go 将新标签写入当前匿名用户的私有词表。题目分析完成后自动应用，不显示“待应用”或确认按钮；详情区只提供常规手动修改入口。

## 7.3 做题、统计与归档的页面边界

导航保留“做题模式 / 学习统计 / 错题归档”三个入口，但三者不共享同一套列表报表：

* 做题模式：面向当前行动。顶部优先展示今日复习、最近答错、薄弱专项、新题巩固、随机混合等可直接开始的练习任务；手动选择仅作为按学科临时组题的补充。作答、即时判分、待批改和完成后跳转单题 AI 解析/追问是主流程，不放掌握度图表。
* 学习统计：面向整体反馈。展示基于练习会话的正确率与近期变化、掌握度分布、薄弱学科/知识点和到期复习数量。AI 洞察只使用已保存的 AI 错因归纳与练习数据；没有数据时明确提示，不虚构趋势。
* 错题归档：面向题目资产管理。提供全文检索、单一学科分类、多个知识点标签、收藏、待校准、复习记录和答错记录筛选；列表点击后回到首页单题工作台，不在归档页堆叠统计报表。

---

# 8. 上传入口设计

## MVP 入口建议

提供两个入口：

* 顶部主按钮：始终可见
* 左栏空状态按钮：首次使用时突出

## 上传方式

支持：

* 点击上传图片
* 拖拽图片上传
* 粘贴截图
* 通过手动创建入口录入题干、题型、选项与答案

文件选择器只接受 `image/*`，不展示 PDF 入口，不根据 PDF 类型切换上传分支，也不展示 PDF 批次、页码或 parser warnings。

多图片批量导入仍是图片批量导入，不是 PDF 拆题。进度条应按文件展示完成数、失败数与总数；任一图片失败时需要显示失败文件或失败数量，但不阻塞其他图片入库。

## 上传后状态

建议明确展示三个状态：

* 上传中
* OCR 识别中
* 解析生成中

避免用户误以为页面卡住。

批量导入不允许永久停在“3/4”。前端轮询需要有超时和局部失败提示；后端每个 batch item 都必须进入 `completed` 或 `failed` 终态。

OCR 结构化结果需要轻量质量提示，不新增复杂批次校对页。当后端返回 `options_incomplete`、`missing_options_*`、`llm_refine_failed`、`llm_refine_unavailable`、`ocr_low_confidence`，或 `structureConfidence < 0.72` 时，题目进入 `qualityStatus = needs_review`。左栏卡片和详情区都可以显示“待校准”，但提示的目的只是帮助用户发现题面结构风险，不应遮挡做题和编辑主流程。用户手动校准保存后，来源可标记为 `manual_corrected`，结构问题已补齐时不再强提醒。

倒计时、`24/31` 题号进度、单选/多选题型、分值、难度和“第 N 题”等考试界面信息不属于题干。后端应在规则结构化和 LLM 二次校验后各清洗一次，详情区默认展示清洗后的题干，同时在“OCR 原文”中保留原始文字。返回 `ocr_exam_ui_noise_removed` 时只做轻量提示，不要求用户重复手工删除；若仍有误，右栏“编辑”允许直接改题干并保存。

---

# 9. 关键交互流

## 9.1 首次使用流

1. 用户进入页面
2. 页面为空状态，引导上传题目
3. 上传后展示处理中状态
4. 识别完成后自动切到三栏工作台
5. AI 解析结果出现在中栏和右栏

## 9.2 查看历史题目流

1. 用户在左栏搜索或筛选
2. 点击题目卡片
3. 中栏加载该题对话历史
4. 右栏加载该题详情与正式解析

## 9.3 OCR 校准流

1. 用户发现识别有误
2. 点击“编辑校准”
3. 在右栏修改题干、题型、选项和答案
4. 保存后刷新题目详情与左栏摘要
5. 如校准影响题意，用户可再手动触发重新解析

对于历史题目中残留的考试界面前缀，用户可直接执行“右栏编辑 → 修改题干 → 保存”；新导入题目由后端自动清洗。

## 9.4 继续追问流

1. 用户阅读标准解析
2. 在中栏输入追问
3. AI 根据当前题上下文回答
4. 用户决定是否将这轮结论沉淀为补充备注

## 9.5 推荐练习流

1. 用户进入做题页，先看到“今日复习 / 最近错题 / 薄弱专项 / 新题巩固 / 随机混合”五个模式。
2. 系统展示推荐数量与简短原因，用户可直接开始；没有推荐题时显示空状态。
3. 练习页按题型渲染输入：单选按钮、多选勾选、判断题正确/错误、填空输入，以及主观题/简答题/论述题/计算题多行文本。
4. 客观题提交后进入基础判分；需人工判断的题型显示“待批改”，不自动判定掌握。
5. 提交后更新 `QuestionLearningState`，错题提前复习，连续答对延后复习。

---

# 10. 页面状态设计

至少考虑以下状态：

## 空状态

* 无题目
* 无搜索结果
* 无对话记录

## 加载状态

* OCR 中
* AI 解析中
* 历史消息加载中

## 异常状态

* OCR 失败
* 解析失败
* 上传失败
* 网络失败

异常状态下必须给出明确后续动作：

* 重试
* 改为手动输入
* 编辑校准

---

# 11. 响应式策略

## 桌面端

优先使用三栏布局，这是主战场。

## 平板端

建议降级为双栏：

* 左栏收起为抽屉
* 中栏与右栏保留

## 手机端

不建议优先做完整功能，但若要兼容，建议：

* 默认显示题目详情
* 对话区和题库区通过底部 Tab 或抽屉切换

MVP 阶段可以明确标注：优先适配桌面端。

---

# 12. 组件清单

建议至少拆出以下组件：

* `AppHeader`
* `QuestionSidebar`
* `QuestionSearchBar`
* `QuestionFilterBar`
* `QuestionList`
* `QuestionListItem`
* `ConversationPanel`
* `AnalysisCard`
* `PromptChips`
* `ChatComposer`
* `ConfirmDialog`
* `QuestionDetailPanel`
* `QuestionStemCard`
* `OptionList`
* `AnswerSummary`
* `AnalysisSections`
* `TagGroup`
* `UploadDialog`
* `ImportProgress`

`ConfirmDialog` 用于题目删除、分类删除等确认操作，统一 title、description、取消、确认删除、danger 与 loading 状态，避免每个业务组件重复写弹窗 JSX。

---

# 13. 给 Stitch 的页面生成提示建议

如果后续把这份文档喂给 Stitch，建议强调以下要求：

* 生成一个深色桌面工作台，不是普通聊天页
* 采用三栏布局：左侧题库，中间对话，右侧题目详情
* 左栏展示题目检索、状态筛选、学科标签和题目列表
* 中栏展示 AI 标准解析卡片和后续追问对话
* 右栏展示题干、选项、正确答案、标准解析、标签和状态操作
* 风格偏专业学习工具，强调信息密度和可读性
* 主强调色使用暖橙铜色，避免常见紫色 AI 风格
* 页面优先适配桌面端 1440px 以上宽度

---

# 14. 最终页面方向结论

综合两个 demo，建议采用以下方案：

* 布局：以 `demo1` 为主
* 对话表达：吸收 `demo2` 的干净消息层级
* 产品定位：以“错题工作台”而不是“AI 聊天页”为核心
* 设计策略：右栏负责沉淀事实，中栏负责解释与追问，左栏负责组织与切换

这样后续无论是做高保真原型，还是直接进入前端开发，方向都会更稳。

---

# 15. P0 单题 Agent 交互状态补充（2026-09-07）

中栏快捷动作只触发显式业务动作，不让模型猜测按钮意图：

* “归纳错因”调用 Go 的学习状态入口；执行中显示“归纳中…”，成功后刷新错因、薄弱标签和复习建议，缺少作答时显示待补充提示，失败时保留原值并提供重试。
* “换种讲法”调用 Go 的 Agent 动作代理，结果以中栏 assistant 消息展示；原解析与题目事实不被覆盖，矛盾时显示待复核。
* “分级提示”默认请求 1 级，返回方向、下一问和不泄露答案的状态；后续可在同一动作契约下增加 2/3 级，不增加独立 Agent 页面。
* AI 分类/标签由 Go 校验后自动应用到尚未人工设置 taxonomy 的题目，解析卡片只标记“AI 已自动分类与打标签”；无可用大类时保持未分类并提示原因，用户可在右栏手动修改，不需要确认或应用。

统一状态包括：执行中、已完成、需要补充、待复核、失败可重试。三栏职责不变：左栏组织题目，中栏解释与动作结果，右栏沉淀题目事实和最终生效的 taxonomy。P1 图片追问、相似题生成、主观题辅助批改不在当前页面承诺范围内。

# 16. 匿名导入与异步状态交互补充（2026-09-10）

首次访问不出现注册阻断。Go 创建匿名会话后，页面显示：“未注册数据仅绑定当前浏览器；清除 Cookie 或更换设备后无法恢复。”前端请求携带 Cookie，页面不提交 userId 作为权限依据。

图片上传成功后立即创建占位题目，右栏不渲染空白解析：`queued` 显示上传/OCR/解析排队，`processing + ocr` 显示 OCR 识别中，OCR 完成立即替换题干、题型、选项和质量 warning，`processing + analysis` 显示“AI 分析中”和“解析中”。题干不可用的 `needs_review` 停在校准；题干可用但置信度不足时继续 AI 并保留待复核 warning；`failed` 显示错误和重试入口。AI 识别到的分类和 tag 在题目尚未人工设置 taxonomy 时默认应用，右栏仍允许手动改回。

若视觉 provider 暂时不可达但文本 provider 可用，解析区继续展示基于 OCR 的真实 AI 结果，并显示“图片理解暂不可用，已基于 OCR 解析”的 warning；两个 provider 都不可用时显示失败原因和重试入口，不能显示 mock 摘要冒充完成。

单题和批量导入使用 1～2 秒起步的可取消轮询，按 job/question 去重，网络失败退避，最长等待后明确提示超时。切换题目、重复上传或卸载组件会取消旧轮询；旧结果不得覆盖当前题目。批量只有所有 item 进入 `completed`、`needs_review` 或 `failed` 才结束进度。
